/// <reference path="../adonis-typings/bodyparser.d.ts" />
/// <reference types="node" />
/**
 * We can detect file types for these files using the magic
 * number
 */
export declare const supportMagicFileTypes: string[];
/**
 * Returns the file `type`, `subtype` and `extension`.
 */
export declare function getFileType(fileContents: Buffer, clientName: string, headers: {
    [key: string]: string;
}, force?: boolean): Promise<null | {
    ext: string;
    type?: string;
    subtype?: string;
}>;
